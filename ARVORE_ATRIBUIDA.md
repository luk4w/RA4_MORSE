# Arvore Sintatica Atribuida

Arvore anotada com a categoria semantica, o operando/valor e o tipo inferido de cada no (resultado de `verificarTipos`).

- **Programa** : _DESCONHECIDO_
  - **Operador aritmetico** `+` : _INT_ (linha 3)
    - **Literal numerico** `8` : _INT_ (linha 3)
    - **Literal numerico** `5` : _INT_ (linha 3)
  - **Operador aritmetico** `-` : _INT_ (linha 4)
    - **Literal numerico** `20` : _INT_ (linha 4)
    - **Literal numerico** `6` : _INT_ (linha 4)
  - **Operador aritmetico** `*` : _INT_ (linha 5)
    - **Literal numerico** `7` : _INT_ (linha 5)
    - **Literal numerico** `4` : _INT_ (linha 5)
  - **Operador aritmetico** `-` : _INT_ (linha 5)
    - **Literal numerico** `6` : _INT_ (linha 5)
    - **Literal numerico** `2` : _INT_ (linha 5)
  - **Operador aritmetico** `/` : _INT_ (linha 6)
    - **Literal numerico** `20` : _INT_ (linha 6)
    - **Literal numerico** `6` : _INT_ (linha 6)
  - **Operador aritmetico** `%` : _INT_ (linha 7)
    - **Literal numerico** `20` : _INT_ (linha 7)
    - **Literal numerico** `6` : _INT_ (linha 7)
  - **Operador aritmetico** `^` : _INT_ (linha 8)
    - **Literal numerico** `2` : _INT_ (linha 8)
    - **Literal numerico** `5` : _INT_ (linha 8)
  - **Operador aritmetico** `+` : _REAL_ (linha 10)
    - **Literal numerico** `3.5` : _REAL_ (linha 10)
    - **Literal numerico** `2.0` : _REAL_ (linha 10)
  - **Operador aritmetico** `|` : _REAL_ (linha 11)
    - **Literal numerico** `9.0` : _REAL_ (linha 11)
    - **Literal numerico** `2.0` : _REAL_ (linha 11)
  - **Definicao de variavel (STORE)** `CONTADOR` : _INT_ (linha 12)
    - **Literal numerico** `50` : _INT_ (linha 12)
  - **Definicao de variavel (STORE)** `TAXA` : _REAL_ (linha 13)
    - **Literal numerico** `3.5` : _REAL_ (linha 13)
  - **Definicao de variavel (STORE)** `ATIVO` : _BOOL_ (linha 14)
    - **Literal logico** `TRUE` : _BOOL_ (linha 14)
  - **Operador aritmetico** `+` : _INT_ (linha 15)
    - **Uso de variavel (LOAD)** `CONTADOR` : _INT_ (linha 15)
    - **Resultado anterior (RES)** `RES` : _INT_ (linha 15)
      - **Literal numerico** `2` : _INT_ (linha 15)
  - **Operador aritmetico** `*` : _REAL_ (linha 16)
    - **Uso de variavel (LOAD)** `TAXA` : _REAL_ (linha 16)
    - **Literal numerico** `2.0` : _REAL_ (linha 16)
  - **Operador relacional** `!=` : _BOOL_ (linha 17)
    - **Uso de variavel (LOAD)** `ATIVO` : _BOOL_ (linha 17)
    - **Literal logico** `FALSE` : _BOOL_ (linha 17)
  - **Decisao (IFELSE)** `IFELSE` : _INT_ (linha 18)
    - **Operador relacional** `>=` : _BOOL_ (linha 18)
      - **Uso de variavel (LOAD)** `CONTADOR` : _INT_ (linha 18)
      - **Literal numerico** `50` : _INT_ (linha 18)
    - **Literal numerico** `100` : _INT_ (linha 18)
    - **Literal numerico** `200` : _INT_ (linha 18)
  - **Repeticao (WHILE)** `WHILE` : _INT_ (linha 19)
    - **Operador relacional** `<` : _BOOL_ (linha 19)
      - **Uso de variavel (LOAD)** `CONTADOR` : _INT_ (linha 19)
      - **Literal numerico** `60` : _INT_ (linha 19)
    - **Definicao de variavel (STORE)** `CONTADOR` : _INT_ (linha 19)
      - **Operador aritmetico** `+` : _INT_ (linha 19)
        - **Uso de variavel (LOAD)** `CONTADOR` : _INT_ (linha 19)
        - **Literal numerico** `1` : _INT_ (linha 19)
  - **Operador relacional** `!=` : _BOOL_ (linha 20)
    - **Literal logico** `TRUE` : _BOOL_ (linha 20)
    - **Literal logico** `FALSE` : _BOOL_ (linha 20)
  - **Definicao de variavel (STORE)** `FINAL` : _INT_ (linha 21)
    - **Operador aritmetico** `^` : _INT_ (linha 21)
      - **Operador aritmetico** `-` : _INT_ (linha 21)
        - **Operador aritmetico** `*` : _INT_ (linha 21)
          - **Literal numerico** `8` : _INT_ (linha 21)
          - **Literal numerico** `2` : _INT_ (linha 21)
        - **Operador aritmetico** `+` : _INT_ (linha 21)
          - **Literal numerico** `3` : _INT_ (linha 21)
          - **Literal numerico** `1` : _INT_ (linha 21)
      - **Literal numerico** `2` : _INT_ (linha 21)
  - **Uso de variavel (LOAD)** `FINAL` : _INT_ (linha 22)
